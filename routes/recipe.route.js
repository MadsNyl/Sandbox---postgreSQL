const express = require('express')
const router = express.Router()

// get all recipes
router.get('/', )

// get recipe by id
router.get('/:id', )

// create recipe
router.post('/', )

// edit recipe
router.put('/:id', )

// delete recipe
router.delete('/:id', )


module.exports = router